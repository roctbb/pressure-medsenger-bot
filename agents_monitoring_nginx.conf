server {
    listen 80;
    server_name monitoring.ai.medsenger.ru;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/home/medsenger/monitoring-medsenger-bot/monitoring_medsenger_bot.sock;
    }
}
